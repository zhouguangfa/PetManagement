# Pet Management System

一个演示 Spring IoC（控制反转）概念的简单宠物管理系统。

## 🎯 **IoC 演示**

这个项目展示了如何通过 Spring 的依赖注入来轻松切换不同的宠物类型，而无需修改业务逻辑代码。

## 🚀 **运行方式**

### 1. 使用 Maven
```bash
mvn spring-boot:run
```

### 2. 构建并运行
```bash
mvn clean package
java -jar target/pet-management-0.0.1-SNAPSHOT.jar
```

## 🔧 **切换宠物类型**

修改 `application.properties` 中的配置：

```properties
# 当前使用 DogService
pet.type=dog

# 可选值: dog, cat, bird
```

## 🌐 **测试 API**

访问 `http://localhost:8080/pet/speak` 查看不同宠物的叫声！

## 💡 **学习要点**

- `@Service` 注解：标记服务类
- `@Autowired`：自动注入依赖  
- 配置驱动：通过配置文件切换实现
- 接口编程：业务代码只依赖接口，不依赖具体实现